# randomGeneratorRunwayML
Random Generator for RunwayML models that take vectors as input

Spin up a local web server to use. Using Bootstrap for CSS / HTML, P5.js and jQuery. 
Modify js/sketch.js and index.html to suit your needs.

Assumes runway port is localhost:8001 - change in JS if need be.
